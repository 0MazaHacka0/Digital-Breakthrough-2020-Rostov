<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />



    <!-- Login Activity -->
    <style name="LoginActivity.GetPassword" parent="Widget.AppCompat.Button.Colored">
        <item name="android:textColor">@color/loginActivityGetPasswordText</item>
        <item name="backgroundTint">@color/loginActivityGetPasswordBackground</item>
        <item name="android:layout_marginLeft">12dp</item>
        <item name="android:layout_marginRight">12dp</item>
    </style>

    <style name="LoginActivity.Login" parent="Widget.AppCompat.Button.Colored">
        <item name="backgroundTint">@color/loginActivityLogin</item>
        <item name="android:layout_marginLeft">12dp</item>
        <item name="android:layout_marginRight">12dp</item>
    </style>

    <style name="LoginActivity.LoginEsia" parent="Widget.AppCompat.Button.Colored">
        <item name="backgroundTint">@color/loginActivityLoginEsia</item>
        <item name="android:layout_marginLeft">12dp</item>
        <item name="android:layout_marginRight">12dp</item>
    </style>

    <!-- Flat Select Activity -->
    <style name="FlatSelectActivity.Select" parent="Widget.AppCompat.Button.Colored">
        <item name="backgroundTint">@color/loginActivityLogin</item>
        <item name="android:layout_marginLeft">12dp</item>
        <item name="android:layout_marginRight">12dp</item>
    </style>

    <!-- Main Activity -->
    <style name="MainActivity.Item" parent="Widget.AppCompat.Button.Borderless">
        <item name="backgroundTint">#FFF</item>

        <item name="android:layout_height">50dp</item>
        <item name="android:gravity">left|center</item>
<!--        <item name="android:textAlignment">left</item>-->
        <item name="android:padding">5dp</item>
        <item name="android:drawablePadding">40dp</item>

        <item name="android:layout_marginTop">7dp</item>
        <item name="android:layout_marginLeft">7dp</item>
        <item name="android:layout_marginRight">7dp</item>
    </style>

</resources>